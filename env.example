# URL do webhook do n8n
# Configure a URL completa do seu webhook aqui
# Copie este arquivo para .env e configure a URL
VITE_WEBHOOK_URL=https://n8n.versa.tec.br/webhook-test/versa_saude_teste

# URL da API local (servidor de eventos)
# Por padrão usa http://localhost:3001
# Altere apenas se o servidor estiver em outra porta ou URL
VITE_API_URL=http://localhost:3001
